-- Database initialization script for NFC API
-- This script will be executed when the PostgreSQL container starts for the first time

-- Create database if not exists (PostgreSQL creates it automatically via environment variables)
-- But we can add any additional setup here if needed

-- Example: Create any additional schemas or extensions
-- CREATE SCHEMA IF NOT EXISTS nfc_schema;
-- CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- The application will handle table creation via JPA/Hibernate
-- with spring.jpa.hibernate.ddl-auto=update

